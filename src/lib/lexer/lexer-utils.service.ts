import { TokenType } from "chevrotain";

// todo : this is a placeholder
type FilterManger = string

export class LexerUtilsService {

  static toLexerTokens(fm: FilterManger): TokenType {
    return {} as any;

    //return  createToken{
    //   name: LexerToken.Identifier,
    //     pattern: /[a-zA-Z]\w*/
    // }
  }

}